"""Packaged data for the TPRM module.

Sub-packages here hold static reference data shipped with
evidentia-core: questionnaire templates, control mappings,
crosswalks, etc. Loaded via ``importlib.resources`` so the
data ships inside the wheel + works under zipimport.
"""
