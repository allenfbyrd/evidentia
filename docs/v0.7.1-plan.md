# Evidentia v0.7.1 — release plan

> Compiled 2026-04-25 as Step 5.B of the v0.7.0 comprehensive pre-tag
> review. Captures HIGH-priority items deferred from Step 4 with
> documented design rationale, plus small follow-ups that fit a 6-8
> week minor-release cycle.
>
> **Status (2026-04-26): SHIPPED as P0-only.** v0.7.1 shipped the AI
> features hardening (P0) headline. P1 (supply-chain polish) and P2
> (documentation polish) and P3 (optional/community-driven) items have
> been **moved to [v0.7.2-plan.md](v0.7.2-plan.md)** so the v0.7.1
> release could focus on the closing of BLOCKER B3 (`risk_statements/`
> + `explain/`) without scope creep. See the
> [Deferred to v0.7.2](#deferred-to-v072) section near the end of this
> document for the cross-link.
>
> Cross-link to: [capability-matrix.md](capability-matrix.md) (Step-4
> findings), [enterprise-grade.md](enterprise-grade.md) (the quality
> bar), [positioning-and-value.md](positioning-and-value.md) §13
> (the bigger v0.8+ direction), [v0.7.2-plan.md](v0.7.2-plan.md) (the
> forward plan capturing items deferred from this release).

---

## Theme

**"AI features hardening + supply-chain polish."**

v0.7.0 hardened the collectors and signing pipeline to enterprise
grade; v0.7.1 brings the AI features (`evidentia-ai`) up to the same
bar, plus a handful of small supply-chain follow-ups identified
during the v0.7.0 RC iteration and Step-4 review.

This is a deliberately scoped "polish" release — none of the v0.8+
"OSS-native AI moat" features (DFAH determinism harness, MCP server,
DSE screenshot validator, etc. — see [positioning-and-value.md §13.2](positioning-and-value.md))
land here. Those need their own scoped cycle.

---

## P0 — AI features enterprise-grade hardening (the headline) — SHIPPED ✅

### Problem

v0.7.0 collectors got the full enterprise-grade treatment: typed
exception hierarchy, `@with_retry` for transient failures,
`CollectionContext` metadata on every output, ECS-8.11 structured
logging via `evidentia_core.audit.logger`, explicit `BLIND_SPOTS`
disclosure lists where applicable. The AI features (risk_statements/,
explain/) did not — they were last touched in v0.4.0 and skipped the
v0.7.0 hardening pass.

Concrete v0.7.0 findings (per [capability-matrix.md](capability-matrix.md)
§3 HIGH bucket):

- 2 BLOCKER B3 violations (bare `except Exception`) in
  `risk_statements/generator.py` lines 173, 227
- Zero unit tests for the entire `risk_statements/` module
- Stdlib `logging` instead of ECS structured logger
- No `@with_retry` for transient LLM failures
- No `CollectionContext`-equivalent metadata on generated artifacts

### 4 design decisions to make first

These benefit from focused thought, not rushed inclusion in v0.7.0.

| # | Decision | Options | Recommendation |
|---|---|---|---|
| D1 | **Context-metadata abstraction for AI outputs** | (a) Repurpose `CollectionContext` (poor semantic fit — risk statements are *generated*, not *collected*; no `collected_at`/`source_system_id`). (b) New `GenerationContext` type carrying `model`, `temperature`, `prompt_hash`, `run_id`, `generated_at`. (c) Inline metadata fields on `RiskStatement` directly. | **(b)** — clean abstraction, mirrors the collector pattern, lives in `evidentia_core.audit.provenance` next to `CollectionContext`. |
| D2 | **Retry strategy for LLM calls** | Instructor's `max_retries` handles JSON validation retries (response wasn't valid Pydantic); `@with_retry` from `evidentia_core.audit.retry` handles network/transient retries (5xx, timeout, rate-limit). They serve different purposes. | **Stack both**: `@with_retry` wraps the LLM call (network layer); Instructor's `max_retries=3` stays as-is (validation layer). Cap `@with_retry` at 3 attempts with exponential backoff. Cite the LiteLLM retry semantics in inline doc. |
| D3 | **EventAction additions for AI events** | New entries in `evidentia_core.audit.events.EventAction` enum. Naming should follow the existing `evidentia.<subsystem>.<verb>` pattern. | Add: `AI_RISK_GENERATED`, `AI_RISK_FAILED`, `AI_RISK_RETRY`, `AI_EXPLAIN_GENERATED`, `AI_EXPLAIN_FAILED`, `AI_EXPLAIN_RETRY`, `AI_EXPLAIN_CACHE_HIT`. Update `docs/log-schema.md` event catalog accordingly. |
| D4 | **Sigstore verify default policy** | Step-4 fix added a structured warning when `expected_identity`/`expected_issuer` not set (UnsafeNoOp fallback). v0.7.1 should consider stronger: emit a `_log.warning(action=VERIFY_SIGNATURE_FAILED)` event in addition to the report warning, OR change default to require identity (breaking API change). | **Emit the structured log warning** in v0.7.1 (additive, non-breaking). Defer the breaking-change require-identity default to v1.0 with a deprecation cycle. |

### Implementation outline

Once D1-D4 are decided:

1. **Add `GenerationContext` to `evidentia_core.audit.provenance`** (~50 lines + tests)
2. **Add the 7 new `EventAction` enum entries** (~10 lines + 1 test entry per `test_events.py`)
3. **Refactor `risk_statements/generator.py`**:
   - Add typed exception hierarchy: `RiskStatementError` → `LLMUnavailableError`, `RiskValidationError`, `RiskGenerationFailed`
   - Replace bare `except Exception` (lines 173, 227) with `except (LLMUnavailableError, RiskValidationError, RiskGenerationFailed) as e`
   - Wrap LLM call with `@with_retry(max_attempts=3)` from `evidentia_core.audit.retry`
   - Use `_log = get_logger("evidentia.ai.risk_statements")` with structured event emission
   - Attach `GenerationContext` to every `RiskStatement` output
4. **Refactor `explain/generator.py`** with the same pattern
5. **New `tests/unit/test_ai/test_risk_statements.py`** (~250-300 lines):
   - Mock `instructor.Instructor.chat.completions.create` via `unittest.mock.patch`
   - Cover sync `generate`, async `generate_async`, `generate_batch`, `generate_batch_async`
   - Cover validation retry (Instructor max_retries hit then succeeds)
   - Cover network retry (`@with_retry` exponential backoff)
   - Cover air-gap refusal (`OfflineViolationError` for cloud models)
   - Cover empty `gaps` list, oversized `gaps` list (1000 entries with rate-limit simulation)
   - Cover `GenerationContext` propagation
6. **Update `tests/unit/test_ai/test_explain.py`** with the new pattern (additive, not breaking existing tests)
7. **Update `docs/enterprise-grade.md`**: clarify that B3 is now closed across BOTH collectors AND AI features

**Estimated scope**: ~600-800 lines of source + test changes. 1-2 weeks of focused work.

---

## P1 — Supply-chain polish (small but high signal) — DEFERRED to v0.7.2

> Moved to [v0.7.2-plan.md](v0.7.2-plan.md) §"P0 — Supply-chain
> hardening" at v0.7.1 ship time. S5 ("Sigstore verify warning log
> emission") was implemented as part of v0.7.1 P0 (commit `dc9f8f4`).
> S6 ("`PYPI_API_TOKEN` deletion") landed during v0.7.0 ship-day
> housekeeping and was verified absent post-v0.7.1 via `gh secret
> list --env=pypi --repo allenfbyrd/evidentia`. The remaining items
> (S1-S4) carry forward to v0.7.2.



| # | Item | Scope | Rationale |
|---|---|---|---|
| S1 | **Composite action SHA-pinning** | Replace `@v5`, `@v4`, `@v2` with `@<40-char-sha> # vX.Y.Z` for all 5 third-party actions in `.github/actions/gap-analysis/action.yml`. Add a Dependabot `actions` ecosystem entry to keep them updated. | OpenSSF Scorecard "Pinned-Dependencies" check; supply-chain hardening for production audit pipelines. ~30 lines + Dependabot config. |
| S2 | **Composite action E2E smoke test** | New `.github/workflows/action-smoke-test.yml` that runs the `./.github/actions/gap-analysis` action against the Meridian sample inventory on every PR. Validates the action.yml CLI surface against the actual `evidentia` CLI. | Catches future CLI/action drift like the `--bundle` vs `--sigstore-bundle` mismatch surfaced in Step 4. ~50 lines. |
| S3 | **SLSA L3 build provenance** | Add `actions/attest-build-provenance@v2` to `.github/workflows/release.yml`. Verify via `gh attestation verify dist/<wheel> -R allenfbyrd/evidentia`. | Closes the H2 enterprise-grade item ("SLSA L2+ reproducible builds + SBOM"); already partial in v0.7.0 (SBOM done). ~20 lines. |
| S4 | **OpenSSF Scorecard weekly workflow** | New `.github/workflows/scorecard.yml` running `ossf/scorecard-action@v2` weekly. Public dashboard at `securityscorecards.dev`. | Visible scoring is marketing for a GRC tool; flags any future regressions in Pinned-Dependencies, Branch-Protection, etc. ~30 lines. |
| S5 | **`evidentia oscal verify` Sigstore identity warning emission** | Step-4 fix added the warning to `VerifyReport.warnings`. v0.7.1 also emits it as a structured log event so SIEM pipelines see it. | Decision D4 above. ~10 lines. |
| S6 | **PYPI_API_TOKEN deletion verification** | After v0.7.0 ships and the OIDC publish flow has run cleanly N+ times, delete the legacy `PYPI_API_TOKEN` GitHub environment secret. Document in CHANGELOG. | Removes the long-lived credential surface area; closes the v0.7.0 supply-chain story. Manual UI step. |

---

## P2 — Documentation polish — DEFERRED to v0.7.2

> Moved to [v0.7.2-plan.md](v0.7.2-plan.md) §"P1 — Documentation refresh"
> at v0.7.1 ship time. DOC1 ("`docs/log-schema.md` AI EventActions
> catalog") **landed in v0.7.1 P0** (commit `dc9f8f4`); the v0.7.2
> entry tracks any drift after S1-S4 add new audit events. DOC4
> ("`docs/ROADMAP.md` v0.7.0 SHIPPED") was completed at v0.7.0 ship
> time. DOC2 / DOC3 / DOC5 carry forward.



| # | Item | Scope |
|---|---|---|
| DOC1 | **Update `docs/log-schema.md` with 7 new AI EventActions** (after D3) | New rows in the event-catalog table; example records for each. ~30 lines. |
| DOC2 | **Add `docs/v0.8.0-plan.md`** drafting the bigger OSS-native AI moat features per `positioning-and-value.md` §13.2 | DFAH determinism harness, PRT integration, MCP server, DSE evidence validator, evidentia-catalogs standalone repo, Hugging Face benchmark dataset. Forward-looking; revisited at v0.7.1 ship time. |
| DOC3 | **Add `docs/sigstore-quickstart.md`** | End-to-end walkthrough: install with `[sigstore]` extra → generate AR → sign with Sigstore → verify in CI → enforce identity policy. Currently the only docs are the `evidentia gap analyze --help` text and the action.yml inline comments. ~150 lines. |
| DOC4 | **Update `docs/ROADMAP.md`** to reflect v0.7.0 SHIPPED + v0.7.1 NEXT | One section update at the top of the roadmap. ~20 lines. |
| DOC5 | **Refresh `Evidentia-Architecture-and-Implementation-Plan.md`** if substantive AI architecture changes occur | After D1+D3 land, Architecture plan section on AI features may need an update. Conditional on architecture changes. |

---

## P3 — Optional / community-driven — DEFERRED to v0.7.2

> Moved verbatim to [v0.7.2-plan.md](v0.7.2-plan.md) §"P2 — Optional
> / community-driven" at v0.7.1 ship time. C1-C6 carry forward
> unchanged.



These are candidates for community contribution after v0.7.0 ships
and the project gains visibility (per the outreach actions in
[positioning-and-value.md §12.5](positioning-and-value.md)):

| # | Item | Stretch goal |
|---|---|---|
| C1 | **Okta collector** | `evidentia collect okta`: MFA enforcement, inactive users, privileged account counts. Maps to AC-2, IA-2, IA-5. Per `docs/ROADMAP.md` "Phase 3 — collectors queue". |
| C2 | **ServiceNow integration** | `evidentia integrations servicenow push --gaps`: push to `sn_compliance_task` via REST with OAuth 2.0. |
| C3 | **Vanta + Drata "push test results" integrations** | Position as "the OSS evidence layer that feeds your existing GRC SaaS". Could be high-uptake among Vanta customers who also want OSCAL outputs. |
| C4 | **Submit to OSCAL Plugfest 2026** | Per `positioning-and-value.md §12.5` outreach actions. |
| C5 | **Open issue on `oscal-compass/community`** proposing compliance-trestle interop | Same source. |
| C6 | **Multi-industry sample-data expansion** | Currently ships 3 fictional scenarios (Meridian fintech / Acme healthtech / Northstar DoD). v0.7.1 adds 4-7 more across retail (PCI DSS), SaaS startup (SOC 2), education (FERPA), banking (NYDFS / GLBA), energy/utilities (NERC CIP), manufacturing (NIST CSF). Each scenario needs: inventory YAML + system-context YAML + walkthrough doc + (optional) pre-generated gap report. **All synthetic — triple-check no real customer data is incorporated.** Per Allen's Step-5.G request from the v0.7.0 review. |

---

## Scope estimation

| Bucket | Effort |
|---|---|
| P0 (AI hardening) | 1-2 weeks focused |
| P1 (supply-chain polish) | 0.5-1 week |
| P2 (documentation) | 2-3 days |
| P3 (community / stretch) | Open-ended; community-driven |

**Realistic v0.7.1 ship date**: 6-8 weeks after v0.7.0, assuming P0
gets the focused attention it needs. Earlier if community helps with
P1/P3.

---

## Post-review additions (v0.7.1 dev branch, 2026-04-25)

After PR #1 + PR #2 of the P0 work landed on the development branch,
a second-pass GRC + cybersecurity adversarial review surfaced four
HIGH and two MEDIUM findings that are being folded into v0.7.1 (rather
than deferred to v0.7.2) because the same fix pattern applies to PR #3
(`explain/`):

- **H1**: `AI_RISK_FAILED` events now carry `run_id` so SIEM queries on
  `evidentia.run_id` surface failures alongside successes.
- **H2**: retry events emitted from inside `_invoke_llm_*` inherit
  `trace.id` from the run_id scope, so retry storms are attributable
  to the batch.
- **H3**: `GenerationContext.credential_identity` field added, populated
  by the new `evidentia_ai.client.get_operator_identity()` helper.
  Closes the NIST AU-3 "Identity" gap for AI-derived artifacts.
- **H4**: CHANGELOG documents the v0.7.0 → v0.7.1 deserialization
  break (same forward-compat property as v0.7.0's
  `SecurityFinding.collection_context` field).
- **M1**: `assert risk is not None` in `_invoke_llm_*` replaced with
  explicit `raise RuntimeError(...)` so the invariant survives `-O`.
- **M2 + M4**: dedicated `AI_RISK_BATCH_COMPLETED` /
  `AI_EXPLAIN_BATCH_COMPLETED` event actions for batch-summary
  emissions; outcome is `success` only when `succeeded == total`,
  `unknown` otherwise (per ECS spec).
- **L2**: `prompt_hash` surfaced in the `AI_RISK_GENERATED` event
  payload so SIEM operators can join log events to artifact-level
  GenerationContext blocks.
- **L4**: model-level test file `tests/unit/test_models/test_risk.py`
  added covering `generation_context` default-None, round-trip
  serialization, and v0.7.0 → v0.7.1 backward-compat deserialization.

The same fixes will be applied to PR #3 (`explain/generator.py`) so
the `explain/` subsystem ships v0.7.1 with the same audit-trail
guarantees.

## Acceptance criteria

v0.7.1 was "shipped" (2026-04-26) with these P0 criteria met:

- [x] `risk_statements/generator.py` has zero `except Exception` (BLOCKER B3 closed) — typed `EvidentiaAIError` hierarchy in `evidentia_ai.exceptions`
- [x] `tests/unit/test_ai/test_risk_statements.py` exists with 772 lines, exercising sync + async + batch + retry + air-gap + GenerationContext
- [x] All AI feature outputs carry `GenerationContext` metadata (sibling of `CollectionContext` in `evidentia_core.audit.provenance`)
- [x] All AI feature events emit through `evidentia_core.audit.logger` (ECS structured)
- [x] `docs/log-schema.md` documents the 9 new `AI_*` EventActions (count grew from the originally-planned 7 after the post-PR review surfaced batch-completed and retry-namespace splits)
- [x] `evidentia oscal verify` emits a structured log warning when Sigstore is verified without expected_identity (S5 from the original P1 list — landed in P0)
- [x] `docs/enterprise-grade.md` updated: B3 + B10 rows clarify AI features are now hardened (commit `dc9f8f4`)
- [x] All tests passing. **Baseline at v0.7.0 tag = 857 passed (8 skipped) on Linux CI** (the canonical CHANGELOG number); the post-P0 development-branch number was 901 collected per the now-resolved memory note. **At v0.7.1 ship: 973 collected** (965 passed + 8 skipped on local Windows; 8 skips are environmental — 6 GnuPG entropy + 2 Sigstore CI-OIDC-only — and pass on Linux CI per the v0.7.0 baseline). Net new tests for P0 ≈ 116 across `test_ai/`, `test_audit/`, `test_models/`.
- [x] mypy strict clean (98 source files)
- [x] ruff lint clean

**Deferred to v0.7.2** (see [Deferred to v0.7.2](#deferred-to-v072) below):

- [ ] `.github/actions/gap-analysis/action.yml` SHA-pins all third-party actions (S1)
- [ ] `.github/workflows/action-smoke-test.yml` exists and is green (S2)
- [ ] `.github/workflows/release.yml` produces SLSA L3 build attestations (S3)
- [ ] `.github/workflows/scorecard.yml` exists and is publishing weekly (S4)
- [ ] `docs/sigstore-quickstart.md` published (DOC3)

**Already complete pre-v0.7.1** (no v0.7.2 follow-through needed):

- [x] `PYPI_API_TOKEN` deleted from `pypi` environment — verified absent post-v0.7.1 via `gh secret list --env=pypi`. Originally listed as a v0.7.1 acceptance item; landed during v0.7.0 ship-day housekeeping.

---

## Deferred to v0.7.2

The deferral decision and the line-by-line carry-forward inventory are
captured in [v0.7.2-plan.md](v0.7.2-plan.md). Headline: P1 (supply-chain
polish — S1-S4), P2 (documentation refresh — DOC2, DOC3, DOC5), and
P3 (optional / community — C1-C6) move to v0.7.2; S5 + DOC1 + DOC4
+ S6 already landed in v0.7.0/v0.7.1 and require no v0.7.2 work.

The cross-link from `docs/ROADMAP.md`'s "v0.7.2 — NEXT" section also
points at the v0.7.2 plan.

---

*End of v0.7.1-plan.md. Status: SHIPPED 2026-04-26 as P0-only.
Pointer in MEMORY.md updated to mark SHIPPED + cross-link to the
v0.7.2 plan for the deferred items.*
