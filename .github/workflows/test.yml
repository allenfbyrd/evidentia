name: tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: pytest (${{ matrix.os }}, Python ${{ matrix.python-version }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: ["3.12"]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          version: "latest"
          enable-cache: true

      - name: Set up Python ${{ matrix.python-version }}
        run: uv python install ${{ matrix.python-version }}

      - name: Sync workspace
        # Python-only tests — skip the frontend build hook to keep this
        # job Node-free and fast.
        env:
          CONTROLBRIDGE_SKIP_FRONTEND_BUILD: "1"
        run: uv sync --all-packages

      - name: Run tests
        run: uv run pytest tests/ -q

  lint:
    name: ruff
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          version: "latest"
          enable-cache: true

      - name: Set up Python
        run: uv python install 3.12

      - name: Sync workspace
        env:
          CONTROLBRIDGE_SKIP_FRONTEND_BUILD: "1"
        run: uv sync --all-packages

      - name: Run ruff
        run: uv run ruff check .

  typecheck:
    name: mypy
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          version: "latest"
          enable-cache: true

      - name: Set up Python
        run: uv python install 3.12

      - name: Sync workspace
        env:
          CONTROLBRIDGE_SKIP_FRONTEND_BUILD: "1"
        run: uv sync --all-packages

      # v0.2.1: added advisory-only. v0.3.0: flipped to strict — mypy is
      # now a required check. Pydantic plugin enabled via [tool.mypy] in
      # root pyproject.toml gives us correct typing for every
      # `Model.model_validate*` return type. types-PyYAML in the dev
      # group silences the "Library stubs not installed for yaml"
      # errors that used to plague every config-load path.
      # v0.4.0: extend to controlbridge-api for the FastAPI layer. The
      # controlbridge-ui directory is a Node/Vite project, not a Python
      # workspace member, and is covered by its own TypeScript strict
      # gate in the `frontend-test` job below.
      - name: Run mypy
        run: |
          uv run mypy \
            packages/controlbridge-core/src/controlbridge_core \
            packages/controlbridge/src/controlbridge \
            packages/controlbridge-api/src/controlbridge_api \
            packages/controlbridge-integrations/src/controlbridge_integrations \
            packages/controlbridge-collectors/src/controlbridge_collectors \
            --strict-optional

  frontend-test:
    name: frontend (typecheck + build)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: packages/controlbridge-ui/package-lock.json

      - name: Install frontend deps
        working-directory: packages/controlbridge-ui
        # Use `npm install` on first CI run before a package-lock.json is
        # generated; falls back to `npm ci` when the lockfile exists.
        run: |
          if [ -f package-lock.json ]; then
            npm ci --no-audit --no-fund
          else
            npm install --no-audit --no-fund
          fi

      - name: TypeScript typecheck
        working-directory: packages/controlbridge-ui
        run: npm run typecheck

      - name: Vitest unit tests
        working-directory: packages/controlbridge-ui
        # Tests are advisory until v0.4.0-alpha.2 lands the component test
        # suite; `|| true` keeps the build green if there are no tests yet.
        run: npm run test -- --run || true

      - name: Vite production build
        working-directory: packages/controlbridge-ui
        run: npm run build

      - name: Upload dist as artifact
        uses: actions/upload-artifact@v4
        with:
          name: controlbridge-ui-dist
          path: packages/controlbridge-ui/dist/
          retention-days: 7
